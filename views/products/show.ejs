<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail Product <%= product.name %></title>
  </head>
  <body>
    <h1><%= product.name %></h1>
    <a href="/products/<%= product._id %>/edit/">Edit Products</a>
    <ul>
      <li>Brand: <%= product.brand %></li>
      <li>Price: <%= rupiah(product.price) %></li>
      <li>Color: <%= product.color %></li>
      <li>
        Category:
        <a href="/products?category=<%= product.category %>">
          <%= product.category %>
        </a>
      </li>
    </ul>
    <h2>
      <a href="/garments/<%= product.garment._id %>"
        ><%= product.garment.name %></a
      >
    </h2>
    <a href="/products/">See All Products</a>
    <form action="/products/<%= product._id %>?_method=DELETE" method="post">
      <button type="submit">Delete</button>
    </form>
  </body>
</html>
